{% extends 'responsive.html.twig' %}

{% block title %}{{ parent() }} - Liste des absences{% endblock %}

{% block body %}
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-sm-8">
                        <h1>Absence étudiant</h1>
                    </div>
                </div>
            </div>
            <div>
                <form method="POST" action="#" enctype="multipart/form-data">
                    <!-- COMPONENT START -->
                    <div class="form-group">
                        <div class="input-group input-file" name="Fichier1">
                            <input type="text" class="form-control" placeholder='Choisissez un fichier' />
                            <span class="input-group-btn">
                                <button class="btn btn-default btn-choose" type="button">Choisir un justificatif</button>
                            </span>
                
                
                        </div>
                    </div>
                    <!-- COMPONENT END -->
                    <div class="form-group">
                        <a type="reset" id="reset" href="#" role="button" class="btn btn-danger">Changer l'excuse</a>
                        <a type="submit" id="submit" href="#" role="button" class="btn btn-primary pull-right">Envoyer le justificatif</a>
                    </div>
                </form>
            </div>

            <table class="table table-bordered m-top-10">
                <thead>
                    <tr>
                        <th>
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" id="checkAll">
                                <label class="custom-control-label" for="checkAll"></label>
                            </div>
                        </th>
                        <th>Du</th>
                        <th>Au</th>
                        <th>Cours</th>
                    </tr>
                </thead>
                <tbody>
                    {% for stuPre in pagination %}
                        <tr>
                            <td>
                                <div class="custom-control custom-checkbox">
                                    <input type="checkbox" class="custom-control-input" id={{stuPre.id}}>
                                    <label class="custom-control-label" for={{stuPre.id}}></label>
                                </div>
                            </td>
                            <td>{{ stuPre.lesson.dateStart|date('d-m-Y h:m') }}</td>
                            <td>{{ stuPre.lesson.dateEnd|date('d-m-Y h:m') }}</td>
                            <td>{{stuPre.lesson.name}}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
{% endblock %}                    