{% extends 'base.html.twig' %}

{% block title %}Admin index{% endblock %}

{% block container %}
    <h1>Admin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admin in admins %}
            <tr>
                <td>{{ admin.username }}</td>
                <td>{{ admin.email }}</td>
                <td>
                    <a class="btn btn-link btn-xs text-info" title="{{ 'see_details'|trans }}" data-toggle="tooltip" href="{{ path('admin_show', {'id': admin.id}) }}"><i class="fa fa-eye" aria-hidden="true"></i></a>
                    <a class="btn btn-link btn-xs text-warning" title="{{ 'edit'|trans }}" data-toggle="tooltip" href="{{ path('admin_edit', {'id': admin.id}) }}"><i class="fa fa-edit" aria-hidden="true"></i></a>
                    {% include 'ui/_delete_form.html.twig' with { id: admin.id, path: 'admin_delete', min: true } %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_new') }}" class="btn btn-success">Create new</a>
{% endblock %}
