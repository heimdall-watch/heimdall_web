{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ student.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ student.email }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ student.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_student_index') }}">back to list</a>

    <a href="{{ path('admin_student_edit', {'id': student.id}) }}">edit</a>

    {{ include('admin/student/_delete_form.html.twig') }}
{% endblock %}
