{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block container %}
    <h1>Edit Student</h1>

    {{ include('student/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('student_index') }}" class="btn btn-secondary">back to list</a>

    {% include 'ui/_delete_form.html.twig' with { id: student.id, path: 'student_delete' } %}
{% endblock %}
