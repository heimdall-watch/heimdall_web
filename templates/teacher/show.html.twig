{% extends 'base.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block container %}
    <h1>Teacher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ teacher.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ teacher.email }}</td>
            </tr>
        </tbody>
    </table>

    <h1>Groupes</h1>

    <table class="table">
        <tbody>
        {% for classGroups in teacher.classGroups%}
            <tr>
                <td>{{ classGroups.name }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('teacher_index') }}" class="btn btn-secondary">Retour à la liste</a>

    <a href="{{ path('teacher_edit', {'id': teacher.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('ui/_delete_form.html.twig', { path: 'teacher_delete', id: teacher.id }) }}
{% endblock %}
